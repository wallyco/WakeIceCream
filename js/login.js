document.getElementById("loginButton").addEventListener("click", loginFunc);

function loginFunc() {
    // Attempts to log in, redirecting to the dashboard with valid email and password.
    // TODO:
    // Check email and password in DB
    // Redirect to dashboard page
    // Alert user on incorrect login info
    let email = document.getElementById("form2Example17").value
    let password = document.getElementById("form2Example27").value
}